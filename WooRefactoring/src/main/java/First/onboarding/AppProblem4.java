package First.onboarding;

import First.onboarding.PhoneBook.MESSAGE;

public class AppProblem4 {
//    private static final int UPPER = 65;
//    private static final int LOWER = 97;
//    private static final int LENGTH = 25;

    public static void main(String[] args) {
//        String word = "I love you";
//        //             0123456789
//        char[] dup = word.toCharArray();
//
//
//        String result = "";
//        for (int i = 0; i < dup.length; i++) {
//            if(word.charAt(i) >= 'A' && word.charAt(i) <= 'Z'){}
//
//            if (Character.isUpperCase(word.charAt(i))) {
//                result += (char) (Math.abs((dup[i] - UPPER) - LENGTH) + UPPER);
//            }
//
//            if (Character.isLowerCase(dup[i])) {
//                result += (char) (Math.abs((dup[i] - LOWER) - LENGTH) + LOWER);
//            }
//
//            if (!(Character.isUpperCase(dup[i])) && !(Character.isLowerCase(dup[i]))) {
//                result += String.valueOf(dup[i]);
//            }
//        }
//        System.out.println(result);

        System.out.println(MESSAGE.START.view());

    }
}